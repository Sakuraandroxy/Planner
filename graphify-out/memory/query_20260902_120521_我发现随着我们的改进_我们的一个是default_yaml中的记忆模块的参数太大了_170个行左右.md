---
type: "query"
date: "2026-09-02T12:05:21.883420+00:00"
question: "我发现随着我们的改进，我们的一个是default.yaml中的记忆模块的参数太大了（170个行左右，远远超出其它配置的参数），这很不符合一个正常好项目的配置文件，还有一个就是runtim.py是不是太长了啊，都已经六千多行了，我感觉是否太大了，将这么多内容塞入一个文件中"
contributor: "graphify"
outcome: "useful"
source_nodes: ["RuntimeObjects", "run_fast_slow_loop()", "AboveStageRuntimeState", "CompletionPipeline", "FastSlowController", "ContinuousPathStream", "_handle_background_target_lost()", "_select_and_transform_plan()"]
---

# Q: 我发现随着我们的改进，我们的一个是default.yaml中的记忆模块的参数太大了（170个行左右，远远超出其它配置的参数），这很不符合一个正常好项目的配置文件，还有一个就是runtim.py是不是太长了啊，都已经六千多行了，我感觉是否太大了，将这么多内容塞入一个文件中

## Answer

Expanded from graph vocabulary: runtime, memory, planning, completion, above, path, controller, relocalization, target, streaming, capture, stage. 结论是两处都需要重构，但问题不只是行数。当前 default.yaml 共 545 行，FUNCTIONS.MEMORY 从第 100 行到第 307 行，包含 207 个配置项，约占整个文件 38%；其中 ABOVE 前缀约 47 项、LARGE_STRUCTURE 约 23 项，并混入路径守卫、AirSim 垂直动作、完成判断、预绑定和重定位参数，说明 MEMORY 已成为跨模块参数收容区。配置还存在 YAML 与代码 get 默认值双重来源、代码使用但 YAML 未声明的 MAX_EVENTS/WORLD_RAY 参数，以及仅为兼容保留但代码不再使用的 ABOVE_MAX_CLEARANCE_M 等参数。runtime.py 当前 6664 行、约 146 个顶层定义，同时负责依赖构造、目标绑定、记忆扫描、above 策略、规划提交、路径守卫、完成判断、目标丢失、路径同步、Web 启动等职责，属于明显的 God Module；mission_memory.py 本身也有 2777 行。建议 default.yaml 仅保留稳定入口和常用覆盖项，按 MEMORY_CORE、TARGET_TRACKING、ABOVE_NAVIGATION、PATH_GUARD、COMPLETION 等所有权拆分组件配置，并使用有类型配置对象作为唯一默认值来源、运行时导出 resolved_config.yaml。runtime.py 保留主循环，依次提取 runtime_context、target_runtime、above_runtime、planning_runtime、completion_runtime 和 web_runtime；controller.py、path_stream.py、completion_pipeline.py 保持现有边界。重构应先建立回归测试和实验日志基线，再做不改行为的函数迁移，最后才调整接口和删除兼容键，避免一次性重写飞行闭环。

## Outcome

- Signal: useful

## Source Nodes

- RuntimeObjects
- run_fast_slow_loop()
- AboveStageRuntimeState
- CompletionPipeline
- FastSlowController
- ContinuousPathStream
- _handle_background_target_lost()
- _select_and_transform_plan()