# 快慢双系统与滑动窗口闭环流程

本文档描述 AirSim 闭环导航的核心实现流程：接到任务后，快系统持续执行当前轨迹队列，慢系统异步调用 Qwen、目标检测、深度观测、距离估计、VLM 完成判定和重定位。核心入口位于 `agent/functions/fast_slow/runtime.py`，轨迹队列与坐标转换位于 `agent/models/planner/sliding_window_planner.py`，连续路径下发位于 `agent/functions/fast_slow/path_stream.py`。

## 1. 总体结构

闭环由两类系统并行协作：

- 快系统：维护世界坐标轨迹队列，将当前队列通过 `moveOnPathAsync` 持续下发给 AirSim，并根据无人机实时位姿消费已到达或已越过的轨迹点。
- 慢系统：当轨迹队列长度进入滑动窗口阈值时异步调用 Qwen；后台周期性做目标检测、深度观测和目标世界坐标估计；当距离触发阈值时停车、清空队列并调用 VLM 做最终完成判定。

关键对象：

- `FastSlowController`：负责 Qwen 请求调度、规划 Future、轨迹队列拼接、队列清空。
- `WorldTrajectoryQueue`：内部保存世界坐标 waypoint，对 Qwen 暴露 incremental body-frame pending。
- `ContinuousPathStream`：负责把当前世界坐标队列同步到 AirSim 的连续路径命令。
- `CompletionPipeline`：后台获取 RGB、深度、检测框，刷新距离估计所需的目标观测。
- `GeometricTargetDistanceEstimator`：缓存目标世界坐标，并用当前无人机世界坐标快速估计到目标距离。
- `TaskCompletionChecker`：在近距离停车后，用可靠检测视图、bbox、深度、估算距离和任务指令调用 VLM 判定任务是否完成。
- `TargetRelocalizer`：目标丢失时扫描重定位，找到则转向目标继续任务，找不到则任务失败。

## 2. 主循环

`run_fast_slow_loop()` 接到任务后先解析任务阶段，然后进入循环。

每个阶段开始时会执行初始化：

- 停止当前 AirSim path stream。
- 清空 Qwen 轨迹队列和待返回的规划任务。
- 清空后台 completion pipeline。
- 清空距离估计器中的目标坐标。
- 清空前端显示的轨迹状态。

循环中每一轮按以下顺序推进：

1. 读取当前无人机位姿，记录导航指标。
2. `path_stream.poll()` 根据当前位姿判断已经消费了多少个队首 waypoint。
3. 如果有 waypoint 被消费，`controller.mark_executed()` 从队列头删除这些点，并标记需要检查缓存目标距离。
4. 如果检测到碰撞，清空队列并执行碰撞恢复。
5. 如果存在缓存目标坐标，计算当前无人机到目标的估算距离。
6. 若估算距离小于触发阈值，立即停止飞行、清空队列、丢弃进行中的慢系统结果，进入近距离完成判定流程。
7. 轮询 Qwen 规划结果，把返回轨迹追加进世界坐标队列。
8. 轮询后台目标观测结果，更新目标世界坐标。
9. 如果轨迹队列长度满足滑窗请求条件，并且当前没有 Qwen 请求在飞，采集规划视图并提交 Qwen。
10. 将最新世界坐标队列同步给 AirSim，使无人机继续沿剩余轨迹不间断飞行。
11. 如果到达后台观测间隔，提交一次目标检测和深度观测任务。

## 3. 滑动窗口 Qwen 规划

Qwen 规划由 `FastSlowController.continuous_planning_decision()` 触发。条件是：

- 快慢系统启用。
- 当前没有未完成的 Qwen Future。
- 当前世界坐标队列长度 `<= max_pending`。
- 当前阶段尚未完成或失败。

提交规划时会采集前视图和下视图，并冻结提交时刻的：

- 无人机世界坐标 `plan_pos`。
- 无人机 yaw `plan_yaw`。
- 当前剩余队列前缀 `pending_world`。
- 当前姿态对应的 pending incremental body-frame waypoint。

传给 Qwen 的提示词必须与滑窗训练格式一致：

```text
Instruction: <stage instruction>
Pending incremental body-frame waypoints: [[...]]   # 仅当 pending 非空时出现
Output exactly 5 additional incremental body-frame waypoints as a JSON list.
Each waypoint must be [dx, dy, dz], where the first pending or output waypoint is relative to the current drone position/front-view frame and each following waypoint is relative to the previous waypoint.
Do not output any other text.
```

Qwen 输入为两张图：

- 前视图。
- 下视图；如果下视图为空则用前视图兜底。

Qwen 输出为 additional incremental body-frame waypoint。即第一个输出点相对“当前 pending 末尾或当前无人机位置”，后续输出点相对前一个输出点。即使 Qwen 输出不足 5 个点，只要能解析出合法 waypoint，也加入队列，不因为数量不足而拒绝。

## 4. 队列拼接与坐标转换

队列内部只保存世界坐标 waypoint，Qwen 只读写 incremental body-frame waypoint。

提交 Qwen 时：

- 当前世界坐标队列前 `max_pending` 个点会通过 `world_to_cumulative_body()` 转为当前机体系累计偏移。
- 再通过 `cumulative_to_incremental()` 转为 Qwen pending。

Qwen 返回时：

- controller 取出提交时冻结的 `pending_world`，而不是使用此刻已经被快系统消费过的实时队列。
- 将冻结 pending 转为提交时刻机体系累计偏移。
- 从 pending 末尾继续累加 Qwen 返回的 additional incremental waypoint。
- 通过 `cumulative_body_to_world()` 转回世界坐标。
- 将新世界坐标 append 到当前队列末尾。

这样可以避免 Qwen 返回时快系统已经飞过一些点，导致追加轨迹的锚点错位。

当前运行时的 body/world 转换采用 yaw-only 旋转矩阵：

- body `dx` 表示当前 yaw 前方。
- body `dy` 表示当前 yaw 右侧。
- body `dz` 直接映射世界 z。
- 不使用 pitch/roll，避免前飞 `dx` 被机体俯仰混入 z 方向造成高度漂移。

## 5. 连续飞行与 moveOnPathAsync

连续飞行由 `ContinuousPathStream.sync()` 负责。它把当前剩余世界坐标队列整体下发给 AirSim，底层对应 `client.start_waypoint_path()`，再由 AirSim client 调用 `moveOnPathAsync`。

`moveOnPathAsync` 的核心参数应表达为：

- `path`：当前剩余的世界坐标 waypoint 列表。
- `velocity`：根据配置和规划状态得到的速度。
- `timeout_sec`：足够大，避免正常飞行被超时中断。
- `drivetrain`：通常保持连续路径飞行所需配置。
- `yaw_mode`：使用保持当前朝向或系统约定的 yaw 行为，避免强制固定 yaw=0 造成掉头。
- `lookahead` / `adaptive_lookahead`：按 AirSim 路径跟随配置使用。

每次 Qwen 返回导致队列尾部增长，或飞行速度变化时，`ContinuousPathStream.sync()` 会重新下发“当前剩余完整队列”。如果只是队首 waypoint 被消费，且新队列正好是旧命令的后缀，则不重新下发，避免反复重置 AirSim 路径导致航向漂移。

为了不间断飞行：

- AirSim 中始终保持一条活动 path。
- Qwen 请求在后台执行，快系统继续飞当前剩余 path。
- 当队列变短到 pending 窗口时提前发起下一次 Qwen。
- Qwen 请求期间 `_continuous_path_velocity()` 会根据剩余路径长度和 `reserve_time_s` 适当降速，给慢系统留出返回时间。
- Qwen 返回后追加轨迹并重新同步 path，使 path 尾部延长。

## 6. 后台目标检测与深度观测

后台观测由 `CompletionPipeline.submit()` 周期性触发。它不会直接完成任务，只负责刷新目标证据和距离估计。

一次后台观测流程：

1. 先采集前视图、下视图 RGB，以及采集时的无人机世界坐标和 yaw。
2. 对两个 RGB 视图分别调用目标检测模型。
3. 对检测结果做可靠性过滤：
   - 不可见则不用。
   - bbox 过大、跨度接近整图或贴边严重会降低可靠性。
   - bbox 横向或纵向跨度达到巨大框阈值时，可靠性为 0，等价于没有可靠目标。
4. 如果两个 RGB 视图都没有可靠目标，立即返回目标丢失事件；主循环停止飞行、清空队列、清空旧目标坐标并进入重定位，不再等待深度图。
5. 如果至少一个 RGB 视图检测到可靠目标，再采集 depth-only 深度图。
6. 将目标 bbox 与深度图结合，计算 bbox 区域的目标深度。
7. 按策略选择一个可靠视图作为距离观测：
   - 普通目标优先前视图。
   - above / over / on top 类阶段优先下视图。
   - 也可通过配置选择 front-only、down-only、best-reliable。
8. 将观测结果作为 `CompletionPipelineEvent("observation")` 返回主循环。

主循环收到 observation 后调用 `_update_target_pose_from_bundle()`，如果存在可靠检测和深度，则更新距离估计器中的目标世界坐标。

如果两个视图都没有可靠目标：

- 不更新目标世界坐标。
- 在需要严格丢失处理的近距离完成触发流程中，会停止、清空队列并进入重定位。
- 对距离估计器而言，目标丢失后应清空目标坐标，避免继续使用旧目标坐标误触发完成。

## 7. 距离估计模块

距离估计模块只在已有可靠目标观测时工作。

第一次检测到目标时：

- 根据检测视图、bbox 中心、相机 FOV、bbox 深度、相机外参偏移、无人机世界坐标和 yaw，估算目标世界坐标。
- 将目标世界坐标缓存到 `GeometricTargetDistanceEstimator`。

后续检测到目标时：

- 使用新的 bbox、深度和无人机位姿更新目标世界坐标。
- 导航指标同步记录目标位置和距离。

如果某次检测没有可靠目标：

- 应清空目标世界坐标。
- 在目标坐标为空时，`estimate_distance()` 返回 `None`，距离估计模块不触发完成判定。

每当快系统消费 waypoint 后，主循环会读取当前无人机世界坐标，并调用 `estimate_distance()` 计算无人机到缓存目标坐标的距离。如果距离小于 `TRIGGER_RADIUS_M`，则触发近距离完成流程：

- 立即停止 AirSim path。
- 清空轨迹队列。
- 丢弃正在进行或之后返回的 Qwen 结果。
- 不再接受旧深度图或旧 Qwen 返回继续影响当前阶段。
- 采集新证据，进入 VLM 完成判定或重定位。

## 8. 近距离 VLM 完成判定

近距离完成判定由 `_handle_distance_completion_trigger()` 负责。

触发后第一步一定是停车和清队列：

- `path_stream.stop()` 停止当前 AirSim path。
- `controller.clear()` 清空世界坐标队列并丢弃 Qwen job。
- `completion_pipeline.clear()` 丢弃后台观测 job。
- 前端轨迹状态清空。

然后重新采集 fresh evidence：

1. 异步请求深度图。
2. 同步请求 RGB 图。
3. RGB 到达后立即对前视图和下视图做目标检测。
4. 如果两个视图都没有可靠目标，等待深度 future 收尾后进入重定位。
5. 如果检测到目标，等待深度图返回，给检测框附加深度。
6. 将可靠检测视图、bbox、深度、置信度、估算距离和任务指令交给 VLM final judge。

VLM 请求只发送可靠目标证据：

- 只发送检测到目标且置信度有效的视图。
- bbox 过大被置为 0 分的视图不发送。
- 不再无条件发送前视图和下视图两张大图，避免多模态 payload 过大或把无效框作为证据。

VLM 判定结果：

- `done=True`：当前阶段完成，清队列，清 pipeline，进入下一个任务阶段；如果所有阶段完成，则整个任务完成。
- `done=False` 且 `target_detected=True`：说明目标还在，但关系或距离条件未完成；清空距离估计，继续任务，下一轮会重新允许 Qwen 规划。
- `target_detected=False` 或 `None`：说明 fresh evidence 丢失目标，应进入重定位，而不是继续沿旧队列飞。

## 9. 重定位

重定位用于目标丢失后的恢复。触发场景包括：

- 近距离完成触发后 fresh RGB 两视图都没有可靠目标。
- VLM 判定失败且目标检测状态不是 true。
- 目标观测链路确认目标丢失，继续使用旧队列会越飞越远。

重定位流程：

1. 停止当前 path。
2. 清空轨迹队列、Qwen job、completion pipeline 和目标坐标。
3. `TargetRelocalizer.search()` 扫描目标。
4. 如果找到目标：
   - 根据目标所在方向旋转无人机朝向目标。
   - 重新采集 fresh RGB 和深度。
   - 重新执行目标检测、距离估计、VLM 判定或 Qwen 规划。
   - 任务继续，滑窗队列从空队列重新开始。
5. 如果没有找到目标：
   - 当前阶段失败。
   - 标记 `task_failed=True`。
   - 结束当前任务循环或进入任务管理器的失败收尾。

重定位成功后，不应继续使用重定位前的队列、旧 Qwen 返回或旧目标坐标。所有慢系统结果都必须以当前阶段和当前状态为准。

## 10. 关键状态机

核心状态可以理解为以下状态机：

```text
任务开始
  -> 阶段初始化
  -> 空队列提交 Qwen
  -> AirSim 执行世界坐标队列
  -> 队列变短时后台提交下一次 Qwen
  -> Qwen 返回后追加队列并重发剩余 path
  -> 后台目标检测和深度刷新目标坐标
  -> 估算距离小于阈值
  -> 停车清队列
  -> fresh 目标检测
       -> 目标存在：等待深度，VLM 判定
            -> done：进入下一阶段或任务完成
            -> not done：清目标坐标，重新规划继续
       -> 目标不存在：重定位
            -> 找到：转向目标，重新进入规划/检测闭环
            -> 找不到：任务失败
```

## 11. 关键日志

运行时常见日志与含义：

- `[PlanInput]`：本次 Qwen 请求的任务、pending 数量、pending waypoint、图像尺寸、提交位姿。
- `[QwenSlidingPrompt]`：实际发给 Qwen 的完整提示词日志，前面用 `<image><image>` 表示双图输入。
- `[Qwen]`：Qwen 返回耗时和 waypoint 数量。
- `[Queue]` / `[WorldQueue]`：Qwen 输出追加后队列长度和世界坐标轨迹。
- `[WorldPath] started/extended`：当前世界坐标队列已经同步给 AirSim path。
- `[FlightPose]`：根据当前位姿消费了多少 waypoint。
- `[CompletionSnapshot]`：后台 RGB+depth 观测采集结果。
- `[TargetDepth]`：检测框结合深度图得到的目标深度。
- `[DistanceEstimate]`：目标世界坐标估计更新。
- `[CompletionTrigger]`：估算距离进入阈值，已经停车并清空队列。
- `[CompletionVLM]`：VLM final judge 的完成结果。
- `[Relocalize]`：目标丢失后的扫描、旋转和恢复结果。
- `[Completion] done=True source=vlm`：阶段被 VLM 确认完成。
- `[Completion] done=False source=relocalization_failed`：重定位失败，阶段失败。

## 12. 实现原则

闭环实现应遵守以下原则：

- Qwen 只负责根据当前双视图、任务指令和 pending 轨迹补充 additional waypoint，不负责完成判定。
- 完成判定只在停车清队列后的 fresh evidence 上进行。
- 距离估计只能使用可靠检测框和深度，目标丢失时必须清空缓存目标坐标。
- 重定位前后的状态必须隔离，不能让旧 Qwen 返回、旧深度图或旧目标坐标继续影响新状态。
- 轨迹队列的唯一执行格式是世界坐标 waypoint，Qwen 的唯一交互格式是 incremental body-frame waypoint。
- 滑窗追加必须使用 Qwen 提交时冻结的 pending 作为锚点，不能用返回时已经被快系统消费过的队列重新解释。
- `moveOnPathAsync` 应始终接收当前剩余世界坐标队列，以连续 path 的方式飞行；只有队列尾部增长、速度变化或需要停车/重定位时才重发或停止。
