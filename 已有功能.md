# 已有功能

> 项目当前已实现的功能清单。按模块分类。

---

## 一、基础设施（agent/common/）

| 功能 | 文件 | 说明 |
|------|------|------|
| **API 预热** | `warmup.py` | 启动时遍历 config 所有 API 端点发送预热请求，消除首次调用的冷启动延迟。自动去重相同 (url, model) 的端点 |
| **图像编码缓存** | `image_encoder.py` | 前视/下视 JPEG base64 只编码一次，detector 和 planner 可选复用。入口层写入缓存，业务模块通过 `get_cached_front_b64()` / `get_cached_down_b64()` 读取 |
| **多阶段任务管理** | `task_manager.py` | TaskParser 输出的多个 TaskStage 按序执行，当前阶段结束后自动切换到下一阶段 |
| **MiMo thinking 禁用** | warmup / planner / detector / task_parser | 所有 API 调用均携带 `extra_body={"thinking": {"type": "disabled"}}`，避免思考模式消耗 token |

## 二、仿真器层（sim/）

| 功能 | 文件 | 说明 |
|------|------|------|
| **AirSim 封装** | `airsim_client.py` | 连接、起飞、双视图抓取（前视+下视+深度）、waypoints 执行（body→世界坐标 → moveOnPathAsync ForwardOnly）、原子动作执行 |
| **后台取帧** | `frame_capturer.py` | daemon 线程持续抓帧推前端，独立 AirSim 连接避免 IOLoop 冲突。浏览器打开即可看到实时画面 |
| **预热抓帧** | `airsim_client.py:warmup_capture()` | 连接后立即抓前视+下视+深度，初始化 GPU 渲染管线，避免第一步 ~15s 冷启动 |

## 三、Agent 核心模块

| 功能 | 文件 | 说明 |
|------|------|------|
| **任务解析** | `agent/task_parser/vlm_task_parser.py` | 用户中文输入 → VLM API → 拆解为英文 TaskStage 列表（target/instruction/relation 均英文） |
| **目标检测** | `agent/detector/vlm_detector.py` | VLM API：前视图 + 英文 target_query → bbox + 深度 |
| **目标检测** | `agent/detector/groundingdino_detector.py` | 本地 GroundingDINO API：前视图 + 英文 target → bbox |
| **方向估计** | `agent/direction/three_dg_estimator.py` | bbox 像素坐标 → 纯数学计算 → yaw 方向分类（纯本地，不调 API） |
| **API 原子规划** | `agent/planner/api_atomic_planner.py` | VLM API：前视+下视+深度+指令 → K 条候选原子动作（含预计算 delta） |
| **Qwen 规划** | `agent/planner/qwen_planner.py` | 微调 Qwen2.5-VL API：前视+下视+指令 → 5 个机体坐标 waypoints |
| **动作→waypoints 统一** | `api_atomic_planner.py:_actions_to_body_waypoints()` | 原子动作转换为机体坐标 waypoints，与 qwen_planner 输出格式统一 |
| **Delta 计算** | `agent/planner/base.py:compute_per_step_deltas()` | 将任意规划器输出转为逐步 (dx,dy,dz,dphi) 供世界模型使用 |

## 四、执行与评测

| 功能 | 文件 | 说明 |
|------|------|------|
| **注册表模式** | `agent/*/__init__.py` | `@register_planner` / `@register_detector` 等装饰器，yaml 配置即插即用，新增实现 3 行代码 |
| **步级指标** | `run_airsim_web.py:StepTracker` | 每步计时（cap/det/plan/vlm/exec），累积距离/速度/论文指标（SR/NE/SPL/OSR/TL） |
| **开环评测** | `eval/openloop_eval.py` | 读数据集第一帧 → 调 API → waypoints 终点 vs GT，不需要 AirSim |
| **闭环评测** | `eval/run_eval.py` | 连 AirSim → 100 步闭环 → 对比 GT，支持单场景和多场景自动切换 |
| **指标追踪** | `eval/metrics.py` | MetricsTracker：NE/SR/OSR/SPL/TL 实时计算 |
| **场景管理** | `eval/scene_manager.py` | 管理 UE4 进程生命周期，多场景自动切换（替代 3DG-VLN 的 AirVLNSimulatorServerTool） |

## 五、Web 前端

| 功能 | 文件 | 说明 |
|------|------|------|
| **仪表盘** | `web/` | Flask + SSE 实时推送，左：前视图，右上：深度图，右下：下视图，底部：状态栏 |
| **任务输入** | `web/frontend/index.html` | 浏览器输入中文任务，POST /task → SharedState → main_loop |
| **线程安全状态** | `web/shared_state.py` | threading.Lock 保护的共享状态总线 |

## 六、配置

| 功能 | 说明 |
|------|------|
| **config/default.yaml** | 全局配置：切换 detector/planner/task_parser 实现，API 地址/端口，仿真器连接 IP |
| **本地/远程切换** | `SIM.AIRSIM_IP` 留空=localhost，填 IP=远程服务器 |

## 七、工具脚本

| 功能 | 文件 | 说明 |
|------|------|------|
| **训练数据提取** | `tools/extract_training_samples.py` | 从 UAV-VLN-FOV 数据集提取 Qwen2.5-VL 微调样本（LLaMA-Factory 兼容格式） |
| **Qwen 微调配置** | `tools/qwen2_5vl_7b_3dgvln_lora.yaml` | LLaMA-Factory LoRA 微调配参 |
