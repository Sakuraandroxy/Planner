# Uni-LaViRA 周报：斜视相机视角适配

**日期：** 2026 年 9 月 2 日  
**本周主题：** 取消规划器对水平前视相机的固定假设，完成斜视视角的几何适配

## 一、工作背景

此前规划器的主要导航、目标绑定和位置估计流程，默认 Camera API 获取的主视图是“水平向前”的。在这一假设下，图像中目标的左右位置可以近似解释为相对方位，深度也可以按固定前视方向转换为三维目标坐标。

但在实际任务中，摄像头可能在 AirSim `settings.json` 中设置为斜向下或完全下视。例如将主摄像头设为向下倾斜 45° 后，图像中的一个像素方向不再等于无人机的水平前向。如果仍使用旧的前视假设，会带来以下问题：

- 目标在图像中的位置无法正确转换为世界方位；
- RGB 检测框与深度点的空间对应容易出错，造成目标三维绑定偏移；
- 前视、斜视和下视观测无法使用同一套位置表达，多视角难以互相补充；
- 飞到目标上方时，前视图逐渐看不到目标，而下视图即使看到目标，也难以直接参与统一的完成判断。

因此，本周的核心目标是建立一套与摄像头具体角度解耦的统一观测模型，使规划器不再假定所有图像都来自水平前视相机。

## 二、本周主要改进

### 1. 建立统一相机几何模型

为每个 Camera API 观测帧补充并统一管理以下信息：

- 相机内参：图像尺寸、焦距、主点和视场角；
- 相机外参：相机在世界坐标系中的位置和旋转；
- 无人机姿态：机体到世界坐标系的旋转关系和当前航向；
- RGB 与深度时间戳：保证三维投影使用的是同一次观测。

基于这些信息，规划器可以将图像像素先转换为相机坐标系射线，再通过当帧真实外参旋转到世界坐标系，不再将“相机光轴”固定当作“无人机水平前方”。

### 2. 使用 AirSim 真实相机姿态

投影链路改为优先使用 AirSim Camera API 每帧返回的相机位置和四元数，而不是仅根据摄像头名称猜测它是前视还是下视。因此，无论 `settings.json` 中的 `Pitch` 设置为 0°、-45° 还是 -90°，都使用同一套投影流程。

### 3. 统一前视、斜视和下视数据结构

将多个摄像头在同一次采集中的 RGB、深度、相机位姿与无人机位姿封装为统一快照。上层的目标检测、方位估计、三维目标记忆和任务完成判断，可以使用每帧自带的几何信息，而不需要为每一种摄像头角度单独编写一套逻辑。

### 4. 补充真实 Camera API 画面的可视化接口

AirSim 窗口中显示的视角不一定等于 Camera API 真实返回的视角。为了方便实验核对，CLI 和 Web 端均保留了可选的相机录制接口，可以保存前视、斜视或下视真实画面，同时记录当帧相机位姿和内参信息。这使得我们可以对照“规划器实际看到的画面”检查目标检测、三维绑定和任务判断结果。

## 三、改进效果

| 对比项 | 改进前 | 改进后 |
| --- | --- | --- |
| 相机角度假设 | 默认主视图水平向前 | 根据每帧真实相机姿态计算 |
| 目标方位 | 主要依赖前视图水平方位 | 从任意视角的像素射线投影到世界坐标系 |
| RGB 与深度 | 斜视时容易因固定外参而产生偏移 | 使用同帧内参、外参和时间戳进行统一投影 |
| 多视角协同 | 前视与下视存在逻辑割裂 | 前视、斜视和下视使用统一帧模型 |
| 实验可视化 | 主要观看 AirSim 窗口，可能与 API 画面不一致 | 可选录制 Camera API 真实画面并保存帧元数据 |

从实验效果看，当主摄像头设置为斜向下视角时，规划器已能根据 Camera API 返回的真实姿态理解画面，而不是继续按水平前视解释。目标检测结果可以形成与相机朝向一致的世界射线，当获得可靠深度时，还可以进一步更新世界坐标系中的三维目标记忆。

同时，下视图不再只是独立的辅助图片，而是具有完整位姿和投影信息的观测源。当无人机接近目标上方、前视图中目标逐渐消失时，下视观测可以为目标居中和 `above` 类任务完成判断提供更合理的几何基础。

## 四、本周验证内容

- 验证不同相机 `Pitch` 下内参、外参和光轴方向的解析；
- 验证前视与下视图像能够携带各自的真实相机位姿；
- 验证 RGB 目标检测框、深度和世界射线之间的统一数据链路；
- 验证 CLI 和 Web 端可选开启 Camera API 画面录制，用于还原规划器的真实视角；
- 补充了任意相机姿态、多视角帧结构和录制接口的聚焦测试。

## 五、当前不足与下一步计划

本周已完成斜视观测的核心几何适配，但还需要继续做完整实验和工程优化：

- 扩展 0°、-30°、-45° 和 -90° 等不同 `Pitch` 的对比实验，评估目标绑定、世界坐标估计和任务成功率；
- 继续验证前视、斜视和下视交叉使用时，同一目标的身份与三维记忆能否稳定保持；
- 完善下视目标的任务理解和完成判断，特别是屋顶、车辆、喷泉等尺寸差异较大的 `above` 任务；
- 优化 Camera API 独立录像链路。当前录制帧率会受规划抓图和模型调用影响，需要将录像采集与规划循环进一步解耦，以获得更连续的实验视频。

## 六、本周总结

本周的改进将规划器从“依赖水平前视图”推进到“按每帧真实相机姿态理解观测”。这项工作不仅解决了斜向下摄像头的适配问题，也为后续前视、斜视和下视的多视角互补、稳定目标记忆以及更可靠的上方到达判断奠定了统一的几何基础。
