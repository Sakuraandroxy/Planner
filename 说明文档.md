# 项目交接说明文档

本文档用于将当前项目交接给后续维护者，重点说明 AirSim 运行、本地部署模型、数据集与评测流程，以及需要提前确认的环境依赖。

## 一、AirSim 运行

1. 先到 [AirSim Releases](https://github.com/microsoft/AirSim/releases) 下载对应的场景包，并按场景说明解压或部署到本地。
2. AirSim 的配置文件路径为：
   `C:\Users\用户名\Documents\AirSim\settings.json`
3. 配置格式请参考仓库根目录下的 `airsim_setting.json`。
4. 需要注意的点：
   - 当前配置是多旋翼模式。
   - 相机配置包含 `front_center` 和 `down_center`。
   - 如果更换场景，需同步检查相机位姿、分辨率和 FOV 是否仍然适配。

## 二、本地部署模型

项目本地推理依赖 3 个核心模型，模型路径和作用如下。

### 1. 滑窗 Qwen

- 用途：输入 0 到 5 个轨迹点，输出固定 5 个轨迹点。
- 轨迹含义：每个轨迹点表示相对于起始位置的位移，再与前一个点作差得到增量。
- 推理后的处理方式：对 Qwen 输出做前缀和，再结合调用 Qwen 时刻的世界坐标，恢复后续轨迹点的世界坐标。
- 模型目录：
  `/data/sakura/models/3DG-VLN-finetuned-sliding_window_2`

### 2. 目标检测模型

- 用途：作为慢系统后台持续监测目标距离。
- 行为逻辑：
  - 没看见目标时执行重定位。
  - 看见目标且距离小于阈值时停止。
  - 随后进入任务完成判定。
- 模型目录：
  `/data/sakura/models/GroundingDINO`

### 3. 任务解析模型

- 用途：将多阶段任务解析成多个子任务。
- 示例：如果输入“先飞到白色车旁，然后飞到红色车旁”，模型会把它拆成多个连续子任务。
- 模型目录：
  `/data/sakura/models/Qwen2.5-7B-Instruct-AWQ`

### 4. 启动方式

以上模型的运行命令都放在各自目录下的 `run.txt` 中。接手者只需要进入对应目录，按照 `run.txt` 执行即可。

## 三、数据集和评测

### 1. 数据集说明

项目包含训练集和测试集：

- 训练集：用于微调 Qwen。
- 测试集：用于验证和评测整体效果。

数据集来源于论文：

- [arXiv:2606.20045](https://arxiv.org/abs/2606.20045)

数据集路径：

- `/data/sakura/data/UAV-VLN-FOV`

微调步骤说明：

- `/data/sakura/data/UAV-VLN-FOV/tools/run.txt`

### 2. 评测代码

评测入口位于：

- `eval\run_eval.py`

由于评测过程中需要频繁切换服务器上的 AirSim 场景，因此需要提前配置 SSH 免密登录。

服务器上的 AirSim 场景路径：

- `/data/sakura/data/TravelUAV_env`

场景来源：

- [OpenUAV](https://prince687028.github.io/OpenUAV/)

## 四、交接前建议确认的事项

1. 本地机器和服务器上的 Python、CUDA、模型权重、AirSim 场景是否已经配齐。
2. `run.txt` 中的绝对路径是否仍然有效。
3. SSH 免密登录是否可用，尤其是评测脚本切换场景时。
4. `airsim_setting.json` 和实际场景是否匹配。
5. 数据集路径和模型路径是否和当前服务器一致。

## 五、补充说明

该项目是面向 AirSim 无人机任务的完整流程，包含任务解析、轨迹生成、目标检测和评测流程。接手后建议先跑通单个模型，再联调完整闭环，最后再做批量评测。
