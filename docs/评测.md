# 离线批量评测流程

> 如何在 Linux 服务器上使用 TravelUAV 仿真环境 + 数据集 + API 系统跑批量评测，得到 SR/NE/SPL 等论文指标。

---

## 一、整体架构

```
┌── Linux 服务器 ─────────────────────────────────────────────┐
│                                                             │
│  TravelUAV 仿真环境 (/data/sakura/data/TravelUAV_env/)      │
│  ├── closeloop_envs/ModularEuropean/LinuxNoEditor/          │
│  │   └── ModularEuropean.sh  ← UE4 地图, 内嵌 AirSim 插件   │
│  ├── closeloop_envs/ModularPark/...                          │
│  └── carla_town_envs/Town01/...                              │
│                                                             │
│  SceneManager 自动管理:                                      │
│    bash ModularEuropean.sh -RenderOffscreen -GraphicsAdapter=0 │
│    → UE4 无窗口后台渲染, AirSim RPC :41451 就绪              │
│                                                             │
│  UAV-VLN-FOV 数据集 (/data/sakura/data/UAV-VLN-FOV/)       │
│  ├── test/       (Seen)                                     │
│  ├── unobject/   (Unseen Object)                            │
│  └── unscene/    (Unseen Map)                               │
│                                                             │
└──────────────────────┬──────────────────────────────────────┘
                       │ AirSim RPC
                       ▼
┌── 评测脚本 (eval/run_eval.py) ──────────────────────────────┐
│                                                             │
│  1. discover_episodes() → 扫描数据集, 按场景分组             │
│  2. SceneManager 逐场景启动 UE4                              │
│  3. 每条轨迹闭环循环:                                        │
│     ┌→ get_dual_view() 抓帧                                 │
│     │→ detector.detect() → bbox                             │
│     │→ planner.plan() → waypoints                           │
│     │→ execute_waypoints() → AirSim 执行                    │
│     │→ 世界坐标距 GT < 10m? → 到达                           │
│     └→ 记录位姿                                              │
│  4. 最终位置 vs GT 目标 → NE/SR/OSR/SPL                     │
│  5. 全部跑完 → 汇总输出                                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、前置条件

### 2.1 仿真器环境

来自 TravelUAV 项目，HuggingFace 下载：

```bash
pip install huggingface_hub
export HF_ENDPOINT=https://hf-mirror.com
hf download wangxiangyu0814/TravelUAV_env \
    --local-dir /data/sakura/data/TravelUAV_env
```

目录结构：

```
/data/sakura/data/TravelUAV_env/
├── carla_town_envs/
│   ├── Town01/LinuxNoEditor/CarlaUE4.sh
│   └── ...
├── closeloop_envs/
│   ├── ModularEuropean/LinuxNoEditor/ModularEuropean.sh
│   ├── ModularPark/LinuxNoEditor/ModularPark.sh
│   └── ...
└── extra_envs/
    ├── BrushifyUrban/LinuxNoEditor/BrushifyUrban.sh
    └── ...
```

`.sh` 不是地图本身，是启动脚本。`SceneManager` 自动扫描所有 `.sh` 建立场景名映射。

### 2.2 数据集

来自 3DG-VLN，百度网盘下载：

> 链接: `https://pan.baidu.com/s/1slWa79ZdNIHid_fwqyhdxA`  提取码: `ymav`

解压到 `/data/sakura/data/UAV-VLN-FOV/`，结构：

```
UAV-VLN-FOV/
├── train/         ← 2228条, 训练用
├── test/          ← 152条,  Seen 场景
├── unobject/      ← 164条,  Unseen Object
├── unscene/       ← 173条,  Unseen Map
└── meta/          ← instructions.json 等

每条轨迹:
  SceneName/traj_uuid/
  ├── mark.json        ← 起始位姿 + GT 目标位置
  ├── obj_des.json      ← 指令文本
  ├── FrontCamera/      ← 预录前视图（训练/开环用）
  ├── DownCamera/       ← 预录下视图
  └── log/              ← 每帧位姿（训练时算 GT waypoints）
```

### 2.3 训练 vs 评测

```
训练:
  输入: 数据集预录图像（固定）
  过程: 模型看图 → 预测waypoints → 与GT waypoints对比 → 反向传播
  输出: 模型权重

评测:
  输入: 仿真器实时渲染图像（每步不同）
  过程: 模型新图 → 预测waypoints → AirSim执行 → 新位置 → 新图 → ...
  输出: 轨迹终点 vs GT目标位置 → SR/NE/SPL
```

**评测不需要预录图像**。图像由仿真器实时渲染，因为模型走出的轨迹和 GT 不同。

---

## 三、评测流程

### 3.1 多场景自动切换（推荐）

一步搞定，无需手动管理 UE4 进程。`SceneManager` 自动扫描场景、启动/杀死 UE4：

```bash
cd /path/to/uni-lavira-code-main

# test split
python eval/run_eval.py \
    --dataset /data/sakura/data/UAV-VLN-FOV/test \
    --env_root /data/sakura/data/TravelUAV_env \
    --gpu 0

# unobject split
python eval/run_eval.py \
    --dataset /data/sakura/data/UAV-VLN-FOV/unobject \
    --env_root /data/sakura/data/TravelUAV_env \
    --gpu 0

# unscene split
python eval/run_eval.py \
    --dataset /data/sakura/data/UAV-VLN-FOV/unscene \
    --env_root /data/sakura/data/TravelUAV_env \
    --gpu 0
```

自动流程：

```
数据集场景列表: [ModularEuropean, ModularPark, Town01, ...]
     │
     ▼ SceneManager._scan_scenes() 扫描 .sh
场景映射: {ModularEuropean → .../ModularEuropean.sh, ...}
     │
     ▼ 逐场景循环
start("ModularEuropean") → bash ...sh -RenderOffscreen -GraphicsAdapter=0
     │ wait 15s → AirSim 就绪
     ▼
跑该场景所有 episode
     │
     ▼ stop() → kill 进程
start("ModularPark") → ...
```

### 3.2 单场景手动模式

```bash
# 先在终端1手动启动 UE4
bash /data/sakura/data/TravelUAV_env/closeloop_envs/ModularEuropean/LinuxNoEditor/ModularEuropean.sh \
    -RenderOffscreen -GraphicsAdapter=0

# 终端2运行评测
python eval/run_eval.py \
    --dataset /data/sakura/data/UAV-VLN-FOV/test \
    --scene ModularEuropean
```

> 单场景模式需要手动管理 UE4 进程。推荐用 3.1 的多场景模式。

### 3.3 到达判定

与 3DG-VLN 一致，使用 **GT 世界坐标距离**：

```python
pos = client.get_pose()[0]
dist_to_gt = |pos - ep["target"]|    # 欧氏距离
if dist_to_gt < 10.0:               # SUCCESS_RADIUS = 10m
    task_done = True
```

不依赖前视图 bbox 深度（飞过头顶也能正确判定到达）。

### 3.4 指标含义

| 指标 | 全称 | 计算 | 说明 |
|------|------|------|------|
| **NE** | Navigation Error | `|P_final - P_gt|` | 终点到 GT 目标的欧氏距离, 越小越好 |
| **SR** | Success Rate | `NE ≤ 10m` | 成功率, 越高越好 |
| **OSR** | Oracle Success Rate | 过程中任何一步曾进入 10m | 宽松成功率 |
| **TL** | Trajectory Length | 累计飞行路径长度 | 所有步位移之和 |
| **SL** | Straight Line | 起点到 GT 目标的直线距离 | |
| **SPL** | Success weighted by Path Length | `SR × SL / max(TL, SL)` | 路径效率, 越高越好 |

原理：纯几何计算，不依赖任何模型或图像。

```
P_final (轨迹终点)  vs  T_gt (mark.json 里的 target.position)
NE = |P_final - T_gt|
SR = NE ≤ 10m
```

### 3.5 开环 vs 闭环

| | eval/openloop_eval.py | eval/run_eval.py |
|---|---|---|
| **需要 AirSim** | ❌ | ✅ |
| **需要 TravelUAV 环境** | ❌ | ✅ |
| **看图次数** | 只看第一帧 | 每步重新看（最多 100 步） |
| **速度** | 秒级/条 | 分钟级/条 |
| **到达判定** | 无（一次性规划终点） | GT 世界坐标距离 < 10m |
| **用途** | 快速筛选、调 prompt | 最终论文指标 |

开环原理：读数据集第一帧 → 调 API → waypoints 终点转世界坐标 → 对比 GT。

```bash
# 笔记本上就能跑，不需要 AirSim
python eval/openloop_eval.py --dataset /data/sakura/data/UAV-VLN-FOV/test
python eval/openloop_eval.py --dataset /data/sakura/data/UAV-VLN-FOV/unobject
python eval/openloop_eval.py --dataset /data/sakura/data/UAV-VLN-FOV/unscene
```

---

## 四、配置切换

`config/default.yaml` 中改 `AIRSIM_IP` 即可切换本地/远程 AirSim：

```yaml
SIM:
  AIRSIM_IP: ""                    # 本地评测 (代码和 UE4 同机)
  AIRSIM_IP: "172.27.143.102"      # 远程评测 (代码在笔记本, UE4 在服务器)
  AIRSIM_PORT: 41451
```

---

## 五、常见问题

### Q: 为什么必须用仿真器实时渲染，不能直接用数据集预录图像？
A: 闭环每步输出 waypoint → 无人机飞到新位置 → 数据集没有新位置的图像 → 卡住。必须仿真器实时渲染。

### Q: `-RenderOffscreen` 看不到画面？
A: 无窗口模式，GPU 渲染到显存，通过 AirSim API 取 numpy 数组。评测不需要看画面。

### Q: 3DG-VLN 和 TravelUAV 为什么能在同一环境跑？
A: 3DG-VLN 借用了 TravelUAV 的 UE4 地图重新采集数据。两者 mark.json 坐标指向同一物理位置。

### Q: 到达判定为什么不用 bbox 深度？
A: 飞过头顶时前视看不到目标 → bbox 深度不可用。GT 世界坐标是唯一的可靠信号，与 3DG-VLN 一致。

### Q: 代码在笔记本还是服务器？
A: 代码放哪都行。改 `AIRSIM_IP` 切换本地/远程。API 服务的 URL 也在 `config/default.yaml` 中配置。
